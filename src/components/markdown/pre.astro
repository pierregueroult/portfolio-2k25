---
interface Props {
  class: string;
  [key: string]: string;
}
const { class: className, ...props } = Astro.props;
---

<pre
  class={`overflow-auto rounded-md bg-slate-800 p-8 font-mono text-sm text-white ${className}`}
  {...props}><slot />
</pre>
