---
interface Props {
  class: string;
  [key: string]: string;
}
const { class: className, ...props } = Astro.props;
---

<pre
  class={`mb-2.5 overflow-auto rounded-md bg-slate-800 p-4 font-mono text-sm text-white ${className}`}
  {...props}><slot />
</pre>
