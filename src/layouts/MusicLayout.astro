---
interface Props {
  title: string;
  subtitle: string;
}

const { title, subtitle } = Astro.props;
---

<div class="mt-8 mb-4">
  <h3 class="text-2xl font-mono">
    <span class="text-accent font-sans mr-2">✦</span>{title}
  </h3>
  <h4 class="text-sm font-mono">{subtitle}</h4>
  <div class="mt-2">  
    <slot />
  </div>
</div>
